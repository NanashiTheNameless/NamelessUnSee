# Development Compose file. Builds the local image from the working tree.
# For the published image, use docker-compose.yml instead.

services:
  app:
    build: .
    env_file: .env
    environment:
      DATA_DIR: /app/data
      PORT: "3000"
      NSFW_SERVICE_URL: http://moderation:8787
    depends_on:
      moderation:
        condition: service_healthy
    volumes:
      # Persists SQLite, reports, and downloaded IP-intelligence databases.
      # Encrypted media is added here only when STORAGE_BACKEND=local; R2 media
      # is uploaded directly after ephemeral staging in the container temp dir.
      - unsee-data:/app/data
    expose:
      - "3000"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/healthz').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 15s
    restart: unless-stopped

  moderation:
    build:
      context: ./moderation-worker
      dockerfile: Dockerfile
    environment:
      PORT: "8787"
      NSFW_MODEL: ${NSFW_MODEL:-onnx-community/nsfw-classifier-ONNX}
      NSFW_SAFETY_MODEL: ${NSFW_SAFETY_MODEL:-OwenElliott/image-safety-classifier-m}
      NSFW_BINARY_MODEL: ${NSFW_BINARY_MODEL:-onnx-community/nsfw_image_detection-ONNX}
      MODEL_CACHE_DIR: /app/models
    volumes:
      - moderation-models:/app/models
    expose:
      - "8787"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:8787/healthz').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 120s
    restart: unless-stopped

  cloudflared:
    image: cloudflare/cloudflared:latest
    command: tunnel --no-autoupdate run
    environment:
      TUNNEL_TOKEN: ${TUNNEL_TOKEN}
    depends_on:
      app:
        condition: service_healthy
    restart: unless-stopped

volumes:
  unsee-data:
  moderation-models:
